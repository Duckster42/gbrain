# Patterns to Adopt
- Contract-first operations (`src/core/operations.ts`).
- Engine interface boundary (`src/core/engine.ts`).
- Two-axis routing (`docs/architecture/brains-and-sources.md`).
- Staged hybrid retrieval + explain (`src/core/search/*`).
- Edge provenance in graph schema (`links` table fields).
- Generation-based cache invalidation.
- Auditable failure logging (`audit/audit-writer.ts`).
