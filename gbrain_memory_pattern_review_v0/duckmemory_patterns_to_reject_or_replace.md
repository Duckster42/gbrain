# DuckMemory Patterns to Reject or Replace
- Reject install/runtime coupling that requires heavy global toolchain assumptions.
- Replace sprawling markdown-only skill dispatch with typed, testable workflow definitions.
- Replace broad script sprawl with compact orchestrator tasks.
- Reject any remote-default ambiguity in trust context propagation.
- Replace implicit background side effects with explicit DAG state transitions.

For every rejection:
- Better replacement is specified above.
- If omitted, classify as not useful for Duckster stack due to complexity/cost ratio.
